services:
  - type: web
    name: donweb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: donweb-db
          property: connectionString

databases:
  - name: donweb-db
    plan: free
